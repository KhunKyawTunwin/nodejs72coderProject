// w

// function somecode(x, y) {
//   return x + y;
// }

// // for (let i = 0; i < 100; i++) {}
// function bigBrother() {
//   function littleBrother() {
//     return "it is me!";
//   }
//   return littleBrother();
//   // function littleBrother() {
//   //   return "no me!";
//   // }
// }
// console.log(bigBrother());

// Promise

/* function add(a, b) {
  return new Promise((resolve, reject) => {
    if (typeof a === "number" && typeof b === "number") {
      let result = a + b;
      resolve(result);
    } else {
      let message = "A and B must be number!";
      reject(message);
    }
  });
}

add(3, 5)
  .then((result) => {
    console.log(result);
  })
  .catch((message) => {
    console.log(message);
  });
 */
